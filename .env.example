# Cloudflare Workers Builds deploys this repository directly.
# Configure the API token in Cloudflare's Worker build settings, not here.

# Local backend development override:
LATEXDO_DATA_ROOT=./storage/dev
PORT=8787
